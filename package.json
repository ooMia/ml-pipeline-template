{
  "name": "toy-job-finder",
  "version": "0.0.2",
  "scripts": {
    "preinstall": "corepack enable",
    "install": "bash install.sh",
    "test": "pytest --sw --nf -v --show-capture=stdout",
    "coverage": "pytest --cov=.",
    "prepare": "husky",
    "format": "prettier --write ."
  },
  "author": "ooMia",
  "license": "MIT",
  "description": "toy-job-finder",
  "engines": {
    "node": "^23.4.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.4.2"
  },
  "packageManager": "pnpm@9.15.1"
}
